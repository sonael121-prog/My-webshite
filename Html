<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI-Native Backend Roadmap | Portfolio</title>
    <link rel="stylesheet" href="style.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Noto+Sans+KR:wght@300;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="logo">AI/SW Dev.</div>
        <ul class="nav-links">
            <li><a href="#jobs">미래유망직무</a></li>
            <li><a href="#competencies">핵심필수역량</a></li>
            <li><a href="#roadmap">나의 4년 로드맵</a></li>
        </ul>
    </nav>

    <!-- Hero Section -->
    <header class="hero">
        <div class="container">
            <h1 class="glitch-text">대체 불가능한<br><span class="highlight">AI-Native</span> Architect</h1>
            <p>2026년 전략 기술 트렌드를 선도하는 차세대 백엔드 엔지니어</p>
        </div>
    </header>

    <!-- Section 1: Future Jobs -->
    <section id="jobs" class="section">
        <div class="container">
            <h2 class="section-title">Future Jobs</h2>
            <div class="job-cards">
                <div class="card">
                    <h3>설계자 (Architect)</h3>
                    <p>AI 네이티브 플랫폼을 통해 최적화된 시스템 구조를 설계합니다.</p>
                </div>
                <div class="card">
                    <h3>통합자 (Synthesist)</h3>
                    <p>LLM과 벡터 DB를 기존 백엔드 생태계에 완벽히 통합합니다.</p>
                </div>
                <div class="card">
                    <h3>선봉장 (Vanguard)</h3>
                    <p>보안과 신뢰 기반의 고성능 서비스를 구축하여 기술 혁신을 이끕니다.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Section 2: Competencies -->
    <section id="competencies" class="section alt-bg">
        <div class="container">
            <h2 class="section-title">Key Competencies</h2>
            <div class="comp-list">
                <div class="comp-item">
                    <span class="badge">TOP 1</span>
                    <h3>AI 에이전틱 오케스트레이션</h3>
                    <p>다중 에이전트 시스템(MAS) 설계 및 AI 에이전트를 활용한 개발 생산성 극대화 역량</p>
                </div>
                <div class="comp-item">
                    <span class="badge">TOP 2</span>
                    <h3>도메인 특화 RAG & 데이터 엔지니어링</h3>
                    <p>벡터 데이터베이스 모델링 및 하이브리드 검색 기반의 고정밀 AI 시스템 구현 능력</p>
                </div>
                <div class="comp-item">
                    <span class="badge">TOP 3</span>
                    <h3>디지털 신뢰 기반 AI 보안 & 거버넌스</h3>
                    <p>데이터 보호, AI 보안 플랫폼(AISP), 디지털 출처 검증을 통한 시스템 신뢰 확보</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Section 3: Roadmap (Timeline) -->
    <section id="roadmap" class="section">
        <div class="container">
            <h2 class="section-title">4-Year Roadmap</h2>
            <div class="timeline">
                <div class="timeline-item">
                    <div class="timeline-content">
                        <span class="year">1학년: 기초 체력 강화</span>
                        <h4>AI-Native 기초</h4>
                        <ul>
                            <li>Java/Kotlin & Python 기초 학습</li>
                            <li>Claude, Cursor 등 AI 코딩 도구 숙련</li>
                            <li>Git 협업 기초 및 AI 협업 프로젝트 진행</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-content">
                        <span class="year">2학년: 인프라 구축</span>
                        <h4>현대적 백엔드 표준</h4>
                        <ul>
                            <li>Spring Framework & RESTful API 설계</li>
                            <li>RDBMS(MySQL) & NoSQL(MongoDB) 모델링</li>
                            <li>대규모 트래픽 처리 모의 시스템 구축</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-content">
                        <span class="year">3학년: 역량 통합</span>
                        <h4>지능형 오케스트레이션</h4>
                        <ul>
                            <li>벡터 DB 활용 하이브리드 검색 구현</li>
                            <li>LangChain 기반 AI 프레임워크 통합</li>
                            <li>도메인 특화 RAG 시스템 개발</li>
                        </ul>
                    </div>
                </div>
                <div class="timeline-item">
                    <div class="timeline-content">
                        <span class="year">4학년: 고성능 최적화</span>
                        <h4>Vanguard 리더십</h4>
                        <ul>
                            <li>분산 환경 벡터 검색 성능 튜닝</li>
                            <li>컨피덴셜 컴퓨팅 기반 AI 보안 적용</li>
                            <li>클라우드(AWS) 배포 및 무결성 검증</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <p>&copy; 2024 AI Backend Student Portfolio. Built for the Future.</p>
    </footer>

    <script src="script.js"></script>
</body>
</html>
2. CSS (style.css)
:root {
    --bg-color: #0a0b10;
    --section-bg: #11131a;
    --text-primary: #ffffff;
    --text-secondary: #a0a0a0;
    --accent-green: #39FF14;
    --accent-blue: #00D4FF;
    --nav-height: 70px;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: var(--bg-color);
    color: var(--text-primary);
    font-family: 'Inter', 'Noto Sans KR', sans-serif;
    line-height: 1.6;
    overflow-x: hidden;
}

.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Navigation */
.navbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: var(--nav-height);
    background: rgba(10, 11, 16, 0.95);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 5%;
    z-index: 1000;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.logo {
    font-weight: 700;
    font-size: 1.5rem;
    color: var(--accent-green);
}

.nav-links {
    display: flex;
    list-style: none;
}

.nav-links li a {
    color: var(--text-primary);
    text-decoration: none;
    margin-left: 30px;
    font-size: 0.95rem;
    transition: color 0.3s;
}

.nav-links li a:hover {
    color: var(--accent-blue);
}

/* Hero Section */
.hero {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background: radial-gradient(circle at center, #1a1c2c 0%, #0a0b10 100%);
}

.hero h1 {
    font-size: 4rem;
    line-height: 1.2;
    margin-bottom: 20px;
}

.highlight {
    color: var(--accent-green);
    text-shadow: 0 0 15px rgba(57, 255, 20, 0.5);
}

.hero p {
    color: var(--text-secondary);
    font-size: 1.2rem;
}

/* Sections */
.section {
    padding: 100px 0;
}

.alt-bg {
    background-color: var(--section-bg);
}

.section-title {
    font-size: 2.5rem;
    margin-bottom: 50px;
    text-align: center;
}

/* Cards */
.job-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
}

.card {
    background: #1c1e26;
    padding: 40px;
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    transition: transform 0.3s;
}

.card:hover {
    transform: translateY(-10px);
    border-color: var(--accent-blue);
}

.card h3 {
    color: var(--accent-blue);
    margin-bottom: 15px;
}

/* Competency List */
.comp-item {
    margin-bottom: 40px;
    padding-left: 20px;
    border-left: 4px solid var(--accent-green);
}

.badge {
    background: var(--accent-green);
    color: black;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: bold;
}

/* Timeline */
.timeline {
    position: relative;
    padding: 20px 0;
}

.timeline::before {
    content: '';
    position: absolute;
    left: 50%;
    width: 2px;
    height: 100%;
    background: rgba(255, 255, 255, 0.1);
}

.timeline-item {
    margin-bottom: 60px;
    width: 100%;
    display: flex;
    justify-content: space-between;
}

.timeline-content {
    width: 45%;
    background: #1c1e26;
    padding: 30px;
    border-radius: 15px;
    position: relative;
}

.timeline-item:nth-child(even) {
    flex-direction: row-reverse;
}

.year {
    color: var(--accent-green);
    font-weight: bold;
    display: block;
    margin-bottom: 10px;
}

/* Footer */
footer {
    text-align: center;
    padding: 50px 0;
    color: var(--text-secondary);
    font-size: 0.9rem;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
}

/* Responsive */
@media (max-width: 768px) {
    .hero h1 { font-size: 2.5rem; }
    .nav-links { display: none; }
    .timeline::before { left: 20px; }
    .timeline-content { width: 90%; margin-left: 40px; }
    .timeline-item, .timeline-item:nth-child(even) { flex-direction: row; }
}
3. JavaScript (script.js)
// Smooth Scrolling for Navigation Links
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        
        const targetId = this.getAttribute('href');
        const targetElement = document.querySelector(targetId);
        
        if (targetElement) {
            window.scrollTo({
                top: targetElement.offsetTop - 70, // Adjust for navbar height
                behavior: 'smooth'
            });
        }
    });
});

// Simple Scroll Animation Observer
const observerOptions = {
    threshold: 0.1
};

const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
        }
    });
}, observerOptions);

document.querySelectorAll('.card, .comp-item, .timeline-item').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'all 0.6s ease-out';
    observer.observe(el);
});
